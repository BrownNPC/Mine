package start

// func (scene *Scene) InitChunk() {
// 	for x := range c.CHUNK_SIZE {
// 		for y := range c.CHUNK_SIZE {
// 			for z := range c.CHUNK_SIZE {
// 				scene.Chunk.Set(x, y, z, Blocks.Dirt)
// 			}
// 		}
// 	}
// }
